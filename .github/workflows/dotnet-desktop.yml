name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag_name:
        description: 'Tag to release (e.g., v1.0.0)'
        required: true

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: windows-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore dependencies
        run: dotnet restore src/BF3LanReforged/BF3LanReforged.csproj

      - name: Publish application
        run: |
          dotnet publish src/BF3LanReforged/BF3LanReforged.csproj -c Release -o ./publish --self-contained true -p:PublishSingleFile=true -p:RuntimeIdentifier=win-x64

      - name: Copy bf3_offline.exe
        run: copy .\src\PatchedLauncher\bf3_offline.exe .\publish\

      - name: Create ZIP archive
        run: Compress-Archive -Path ./publish/* -DestinationPath ./BF3_LAN_Launcher.zip

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.tag_name || github.ref_name }}
          files: ./BF3_LAN_Launcher.zip
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
